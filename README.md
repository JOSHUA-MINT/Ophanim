# Ophanim
Ophanim is a zero-knowledge, end-to-end PGP encrypted web messenger. All keypairs (ECC Curve25519) are generated in the browser, private keys stay in local storage, and messages are encrypted before transmission. The relay server sees only unreadable ciphertext. Zero accounts, zero database, zero server persistence.
